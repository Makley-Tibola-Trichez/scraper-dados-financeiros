{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python (uv)",
            "type": "python",
            "request": "launch",
            "module": "dados_financeiros.main",
            "console": "integratedTerminal",
            "env": {
                "VIRTUAL_ENV": "${workspaceFolder}/.venv"
            }
        }
    ]
}